
To remove bad entries, i used the dropna() method to remove all rows with NaN and then I removed the corrupted data in 3 ways

1.) removed all datat with negative tree length values

2.) removed all data where leaf size and the sum of the three sub leaf sizes arent close(close i defined as 1% error)

3.) removed all all the unbelievable latitudes and longitudes(like latitude below - 90 and above 80 because you aint finding a tree in north pole and similiarly for longitude)

after that I decided tyo run some tests to see relations

I plotted Tree height v/s Tree Trunk Width and noticed that it seemed like short trees(below 100m) follow a different rule than tall trees(above 100m) and so i decided to analyse them separately

My seperate analysis showed that 